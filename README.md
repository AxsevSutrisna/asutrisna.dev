# asutrisna.dev

A minimal Next.js + React + Tailwind project scaffold for `asutrisna.dev`.

Getting started:

```bash
cd "d:/Working Space/Portofolio/portfolio-next"
npm install
npm run dev
```

This creates a simple homepage and Tailwind setup. Edit `pages/index.js` and `styles/globals.css` to continue.
